<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Bundle identifiers - like package.json "name" -->
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>

    <!-- IMPORTANT: This makes it a menu bar-only app (no dock icon) -->
    <!-- Think of it like "background: true" in Electron apps -->
    <key>LSUIElement</key>
    <true/>

    <!-- Minimum macOS version -->
    <key>LSMinimumSystemVersion</key>
    <string>$(MACOSX_DEPLOYMENT_TARGET)</string>

    <!-- Required: Microphone usage description (shown in permission dialog) -->
    <key>NSMicrophoneUsageDescription</key>
    <string>Yapper needs microphone access to transcribe your speech to text.</string>

    <!-- App category in App Store / Finder -->
    <key>LSApplicationCategoryType</key>
    <string>public.app-category.productivity</string>

    <!-- Supports high-resolution displays -->
    <key>NSHighResolutionCapable</key>
    <true/>

    <!-- Required for SwiftUI lifecycle -->
    <key>NSPrincipalClass</key>
    <string>NSApplication</string>

</dict>
</plist>
